---

import Layout from '~/layouts/PageLayout.astro';
import MedalRank from '~/components/medal-rank.astro';

const metadata = {
  title: 'สรุปอันดับเหรียญ | นาคาเกมส์',
};

---

<Layout metadata={metadata}>
  <MedalRank />
</Layout>
